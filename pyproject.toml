[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "titan-scanner"
version = "1.0.0"
description = "Titan — Autonomous Penetration Testing Platform"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Titan Security Lab"},
]
keywords = ["security", "penetration-testing", "vulnerability-scanner", "dast", "autopentest"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
]
dependencies = [
    "aiohttp>=3.9.0",
    "pyyaml>=6.0",
    "requests>=2.31.0",
    "cryptography>=41.0.0",
    "PyJWT>=2.8.0",
    "python-json-logger>=4.0",
]

[project.optional-dependencies]
full = [
    "playwright>=1.40.0",
    "flask>=3.0.0",
]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
]

[project.scripts]
tscan = "titan.tscan_main:main"

[project.urls]
Homepage = "https://github.com/titan-security-lab/titan"
Documentation = "https://github.com/titan-security-lab/titan#readme"
Repository = "https://github.com/titan-security-lab/titan"

[tool.setuptools.packages.find]
include = ["titan*"]
exclude = ["tests*", "docs*", "titan_states*", "titan_logs*", "titan_output*"]

[tool.setuptools.package-data]
"*" = ["*.md", "*.yaml", "*.yml", "*.json"]

# ---------------------------------------------------------------------------
# Ruff (linter + formatter)
# ---------------------------------------------------------------------------
[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "S",   # flake8-bandit (security)
    "T20", # flake8-print
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "T201",   # print found (scanner uses print for output)
    "S101",   # assert used (fine in tests)
    "S603",   # subprocess call (scanner runs tools)
    "S607",   # start process with partial path
    "B905",   # zip without strict= (Python 3.10 compat)
    "UP007",  # use X | Y for union (Python 3.10 compat)
    # --- Tracked debt: fold back in as each family is hardened (the check
    # only gates what the tree genuinely satisfies; same pattern as the mypy
    # exclude list). Delete the entries as the fixes land. ---
    "S311",   # non-crypto random — payload/UA jitter is intentional in a fuzzer
    "S310",   # urllib — recon/download code uses urllib by design
    "S108",   # hardcoded temp file — fixture/scratch paths in scripts
    "S602",   # subprocess with shell=True — controlled internal invocations
    "S104",   # bind 0.0.0.0 — local_lab listens on all interfaces by design
    "S105",   # hardcoded password — lab + test fixtures only; detect-secrets covers real secrets
    "S324",   # weak hash (md5) — lab simulates legacy auth; detector doesn't use it
    "RUF012", # mutable class defaults — shared-state audit campaign
    "RUF059", "RUF015", "RUF005", "RUF001", "RUF002", "RUF003", "RUF013",
    "SIM102", "SIM105", "SIM103", "SIM108", "SIM110", "SIM115", "SIM116", "SIM117", "SIM222",
    "E402",   # legacy module-level imports after CLI code (entry scripts)
    "E701",   # one-line if/else in legacy scripts
    "E741",   # ambiguous names (l/O) — legacy
    "E721",   # type() comparison — legacy (oracles.py fixed; remaining are fixture code)
    "B007",   # unused loop control var — rename-to-_ campaign
    "B023",   # function uses loop variable — closure-var refactor campaign
    "UP031",  # printf-style % formatting — legacy
    "UP035",  # deprecated imports — migration campaign
    "N806",   # non-lowercase function-local var — legacy naming
    "N802",   # invalid function name (camelCase) — legacy
    "N814",   # camelCase imported as constant — legacy
    "F811",   # redefined-while-unused — fixed in titan_gov.py; fixtures remain
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "T201"]
# F405: names come from `from oracle_shared import *` (shared fixtures/apps).
"tests/test_oracle_detectors.py" = ["F405"]
"titan/local_lab/*" = ["S101"]
"titan/ai/*" = ["T201"]

# ---------------------------------------------------------------------------
# Mypy (type checker)
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
check_untyped_defs = false
disable_error_code = ["import-untyped"]


# Incremental strict gate: the modules below are not yet mypy-clean.
# They are excluded from the check and their types are skipped when
# imported by clean modules. As each module is cleaned, delete its two
# entries (the exclude line and its override block) to fold it into the
# gate. Removing every entry restores full-tree checking.
exclude = [
    "provider.py",
    "titan/ai/payloadforge.py",
    "titan/ai/payloadsmith.py",
    "titan/ai/pg.py",
    "titan/ai/waf_fingerprint.py",
    "titan/brain/strategy.py",
    "titan/cli.py",
    "titan/consent/flags.py",
    "titan/core/auth.py",
    "titan/core/authorization.py",
    "titan/core/discovery.py",
    "titan/core/osint.py",
    "titan/core/stealth.py",
    "titan/core/transport_mixin.py",
    "titan/core/webhooks.py",
    "titan/exploit/consent.py",
    "titan/exploit/listener.py",
    "titan/exploit/reattach.py",
    "titan/exploit/redirect_poc.py",
    "titan/exploit/repl.py",
    "titan/fleet/agents.py",
    "titan/fleet/coordinator.py",
    "titan/hostile/__init__.py",
    "titan/hostile/offense.py",
    "titan/hostile/profiler.py",
    "titan/modules/api/detector.py",
    "titan/modules/baas/appwrite.py",
    "titan/modules/baas/authservices.py",
    "titan/modules/baas/detector.py",
    "titan/modules/baas/fingerprint.py",
    "titan/modules/baas/firebase.py",
    "titan/modules/baas/supabase.py",
    "titan/modules/clientside/prototype/detector.py",
    "titan/modules/cloud/storage.py",
    "titan/modules/cloud_control/imds.py",
    "titan/modules/cors/detector.py",
    "titan/modules/coverage/proof.py",
    "titan/modules/coverage/report.py",
    "titan/modules/coverage/scorer.py",
    "titan/modules/coverage/surfacemapper.py",
    "titan/modules/coverage/tracker.py",
    "titan/modules/deep_audit/prober.py",
    "titan/modules/dns/detector.py",
    "titan/modules/jwt/detector.py",
    "titan/modules/logic/detector.py",
    "titan/modules/postexploit/chainbuilder.py",
    "titan/modules/postexploit/exploitgen.py",
    "titan/modules/postexploit/killchain.py",
    "titan/modules/ratelimit/detector.py",
    "titan/modules/sqli/detector.py",
    "titan/modules/subdomain_takeover/detector.py",
    "titan/modules/supplychain/detector.py",
    "titan/modules/tls/detector.py",
    "titan/modules/upload/detector.py",
    "titan/modules/websocket/detector.py",
    "titan/reporting/__init__.py",
    "titan/reporting/dashboard.py",
    "titan/reporting/estate.py",
    "titan/reporting/markdown_report.py",
    "titan/reporting/remediation.py",
    "titan/stealth/advanced.py",
    "titan/transport/grpc.py",
    "titan/transport/tor.py",
    "titan/transport/websocket.py",
    "titan/verify/__init__.py",
    "titan/verify/ai_escalation.py",
    "titan/verify/auto_verify.py",
    "titan/verify/kernel.py",
]

[[tool.mypy.overrides]]
module = "titan.ai.payloadforge"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.ai.payloadsmith"
follow_imports = "skip"


[[tool.mypy.overrides]]
module = "titan.ai.waf_fingerprint"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.brain.strategy"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.cli"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.consent.flags"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.auth"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.authorization"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.discovery"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.osint"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.stealth"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.transport_mixin"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.core.webhooks"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.exploit.consent"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.exploit.listener"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.exploit.reattach"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.exploit.redirect_poc"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.exploit.repl"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.fleet.agents"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.fleet.coordinator"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.hostile"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.hostile.offense"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.hostile.profiler"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.api.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.appwrite"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.authservices"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.fingerprint"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.firebase"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.baas.supabase"
follow_imports = "skip"









[[tool.mypy.overrides]]
module = "titan.modules.clientside.prototype.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.cloud.storage"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.cloud_control.imds"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.cors.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.coverage.proof"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.coverage.report"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.coverage.scorer"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.coverage.surfacemapper"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.coverage.tracker"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.deep_audit.prober"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.dns.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.jwt.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.logic.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.postexploit.chainbuilder"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.postexploit.exploitgen"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.postexploit.killchain"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.ratelimit.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.sqli.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.subdomain_takeover.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.supplychain.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.tls.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.upload.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.modules.websocket.detector"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.reporting"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.reporting.estate"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.reporting.markdown_report"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.reporting.remediation"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.stealth.advanced"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.transport.grpc"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.transport.tor"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.transport.websocket"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.verify"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.verify.ai_escalation"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.verify.auto_verify"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "titan.verify.kernel"
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "provider"
follow_imports = "skip"

# ---------------------------------------------------------------------------
# Pytest
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

# ---------------------------------------------------------------------------
# Coverage
# ---------------------------------------------------------------------------
[tool.coverage.run]
source = ["titan"]
omit = ["local_lab/*", "tests/*", "*/titan/transport/test_onion_scan.py"]

[tool.coverage.report]
show_missing = true
# Same omit list as [tool.coverage.run] so the standalone `coverage report`
# path (and the CI pytest --cov path) exclude the same files.
omit = ["local_lab/*", "tests/*", "*/titan/transport/test_onion_scan.py"]
# Measured floor is ~45% (full suite, 2026-09-07). Gated at 44 to absorb
# run-to-run variance across CI Python versions; raise this over time.
fail_under = 55

