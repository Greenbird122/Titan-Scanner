# Runtime + dev dependencies for Titan Scanner.
# Source of truth for pip-compile: pinned output lands in requirements.txt.
# Keep this file in sync with pyproject.toml's dependency lists.
playwright==1.62.0
aiohttp
pyyaml
requests
flask
PyJWT
cryptography
pytest
pytest-asyncio
backports-asyncio-runner==1.2.0 ; python_version < "3.11"